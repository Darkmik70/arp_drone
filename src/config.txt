# Write in the following format (TYPE IP:PORT)

# TYPES
# server: This machine will wait for another to sent targets/obstacles data
# client: Our targets/obstacles processes will send to another machine their data.
# standalone: Game is played entirely on local machine.

# IP:PORT (i.e 192.168.1.20:2000)
# For server write any IP and a port
# For client write the IP of the remote machine and its port
# For standalone write localhost and a port

standalone localhost:2000



