#include "obstacles.h"
#include <time.h>
#include <stdlib.h>



int main() 
{
    // This initialization will take place only once
    srand(time(NULL));   

    // Get values for drone position
    int drone_x = 0, drone_y = 0;
    int window_x = 20, window_y = 20;
    
    int obstacles = 20;
    targets = 20;

    int r = rand();      // Returns a pseudo-random integer between 0 and RAND_MAX.



}
